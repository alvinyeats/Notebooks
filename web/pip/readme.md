# pip config

- global: "/etc/pip.conf"
- local user: "~/.pip/pip.conf"

```conf
	[global]
	index-url=http://mirrors.aliyun.com/pypi/simple/

	[install]
	trusted-host=mirrors.aliyun.com

	[list]
	format=columns
```